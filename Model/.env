# MSA inference service config. Read by MSASettings (env prefix `MSA_`).
# Point MSA_MODEL_PATH at whatever checkpoint actually exists after training.
MSA_MODEL_PATH=checkpoints/msa_model_v1/best_model
MSA_DEVICE=cpu
